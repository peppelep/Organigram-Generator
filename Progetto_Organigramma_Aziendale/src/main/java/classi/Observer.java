package classi;

/**
 * Interfaccia Observer che definisce il contratto per gli oggetti osservatori.
 */
public interface Observer {

    /**
     * Metodo chiamato quando viene aggiornato lo stato dell'oggetto osservato.
     */
    void update();
}